# CMD lib

This `lib` module doesn't contains any compilable code. It only contains library
modules to use in other compilable entrypoints for financo.

This `lib` module is inside the `cmd` directory because its modules are only
usable for `cmd` module. While the `lib` module in the repository's root is
for modules to be use in all of **financo**.
